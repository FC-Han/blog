# 2026-08-20 工作日志

## 任务：子页面排版升级（用户反馈日记页难看）
- 用户：子页面（尤其日记页）排版太难看，要求优化。

## 已完成
- **日记页全新设计**（重点）：
  - 顶部统计条：📔 篇日记 / 🗓️ 个月份 / ✍️ 字记录（自动统计）
  - 月份卡片化：每个月份一张卡片（渐变头部 + 年份小字 + 月份大字渐变 + 篇数徽章）
  - 日期徽章：暖橙渐变圆角方块显示日号（如 19），hover 高亮 + 箭头滑入
- **文章列表卡片**：日期改为胶囊徽章、卡片加阴影、hover 浮起更精致
- **标签页/关于页**：统一卡片阴影与悬停效果
- 技术：PostList.vue archive 模式重写为月份卡片布局；posts.js 增加 wordCount（正文去空白字数）；style.css 全量重写（引入 --warm-grad 渐变变量、--card-shadow 阴影变量）

## 关键坑（新增）
- **残留 dev server 也会卡死构建**：昨晚 `npm run docs:dev` 没杀（PID 355），今天 `npm run docs:build` 卡在 building bundles 2 分钟+。**构建前必查 `ps -ef | grep -E "docs:dev|docs:build"`，残留的 dev server 也要杀掉**，再清 .temp/.cache/dist。
- worktree 部署流程已固化：fetch → worktree add --detach（实际路径 /h/h/ghpages2）→ git rm → cp dist → 强推+SHA 校验。残留目录用 `rm -rf /h/h/ghpages2` + `git worktree prune` 清理。

## 部署
- main：b9f22e8（一次推送成功）；gh-pages：516a1fa（一次推送成功）
- 线上验证 ✅：日记页统计条/月份卡片/日期徽章全部渲染，所有页面 200

## 下午补充：新日记 + 搭建文章改写 + 标签页交互式去重（16:40 部署）
- **用户需求**：① 子界面再美化；② 搭建文章改为「借助智能体搭建，并让它整理每天的工作、做好记录」；③ 重复文章不要重复显示；④ 写今天日记（改论文痛苦、过度依赖 AI 变懒、内容太多难整理主线）。
- **内容**：
  - 新日记 `posts/diary-20260820.md`「改论文改到怀疑人生」（tags:[日记]）：AI 依赖纠结（加速器不是代驾）、主线是做出不是选出的。
  - 搭建文章改写：新增「借助智能体搭建」「让智能体整理每天的工作」两节，强调「把怎么做交给 AI，把做什么好不好留给自己」。
- **标签页去重（关键）**：TagList.vue 从「按标签分组全展开」（同一篇多标签会重复出现）改为**交互式筛选**——默认显示全部文章（去重列表，复用 post-card 卡片），点击标签 chip 只看该类，chip 有 active 选中态；标题显示当前筛选「全部内容（N 篇）/ #标签（N 篇）」。style.css 删除旧 tag-section/tag-post-list 样式，新增 .cloud-chip.active、.tag-result-title。
- **日记页再美化**：月份卡片头加 📅 图标（year 单独一行）、卡片 hover 浮起、统计图标彩色底圆（44px）。
- **构建坑**：这次只清 .temp/.cache 不够，**必须连 dist 一起删**（build 卡在 bundles 时，用 PowerShell `Get-Process node | Stop-Process -Force` 全杀 node + 删 .temp/.cache/dist 后重建 7.5s 完成）。
- **部署**：main c429abc + gh-pages 6492ac2（均一次成功）。线上验证 ✅：日记页 8 月两篇（20/19 日徽章）、标签页 5 chip + 3 篇文章各一次无重复、所有页面 200。
