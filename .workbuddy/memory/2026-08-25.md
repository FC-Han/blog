# 2026-08-25 工作日志

## 重写 README.md
- 用户要求把 GitHub 仓库 `FC-Han/blog` 的 README.md 改得漂亮一点、网页链接写对。
- 重写为美观版：顶部徽章（VitePress / GitHub Pages / Giscus / last-commit）、标题区「成的小屋」、特性列表、准确目录结构、本地预览/写文章/构建部署说明、技术栈表格、评论区说明、页脚。
- 修正了原 README 的多处过时/错误信息：
  - 删除不存在的 `deploy.sh` 与 `../blog_deploy` 部署仓库说法；
  - 本地预览端口由 `http://localhost:5173` 修正为 `http://localhost:5173/blog/`（base 是 `/blog/`）；
  - 补充了 Giscus 评论区、目录式页面转换（`scripts/ghpages-html.js` 修复子页 404）、实际目录结构。
- 在线博客链接统一为 `https://fc-han.github.io/blog/`（准确）。
- 提交到 main（`a38e533`）并推送成功（`a766e4b..a38e533`），GitHub 仓库 README 已更新。README 仅影响仓库展示、不影响站点（gh-pages 无需重新部署）。
- 删除了本次的临时推送脚本 `_push_readme.sh`。

## 新增 2026-08-25 日记（晚）
- 用户要求写当天日记：论文基本写完（前言重写、方法待深改、结果与讨论主要 AI 写偏粗糙、图都画好很漂亮）；物种分布模型智能体跑通但效果差强人意，已优化解决不少问题，明天再优化；晚安。
- 新建 `posts/diary-20260825.md`，构建部署。
- 部署坑：后台推送脚本用 `git -C /h/h/ghpages2` 在普通沙箱下报 "No such file or directory"（ghpages2 路径虚拟化不稳定）。改用**从 H:/blog 用 commit hash 强推 gh-pages**（`git push --force origin <hash>:refs/heads/gh-pages`，对象库共享），普通沙箱即可访问，无需依赖 ghpages2 路径。已记入本日志，下次部署可直接用此方式。
- 推送成功：gh-pages `8f0a5aa..d1a4f1f`、main `a38e533..2b9fd54`。线上新日记 200，内容/评论区均正常。
- 清理临时推送脚本 `_push_0825.sh`、`_push_0825b.sh`。
